# Coding Style Guide

Use modern JavaScript (ES6+).

Use async/await instead of callbacks or .then().

Use `const` and `let` instead of `var`.

Use arrow functions where applicable.

Name files and variables in camelCase.

Export modules using `module.exports` or ES Modules depending on context.
